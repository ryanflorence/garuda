# WARNING:
#     Do not edit this file directly
#     You must clone the repository and push


# # Define where you keep your repositories, must be absolute
repositories: /absolute/path/to/repositories

# # Default scripts to run on all repositories and branches when pushed

# # These will run before any other scripts
# post-receive:
#   all: rss
#   heads:
#     master: some_script
#   tags:
#     '^RC.+': some_script

# # These will run after the branch or tag has been extracted
# # But before they are deployed anywhere (before the script defined
# # in repos.yml is called)
# pre-deploy:
#   all: some_script
#   heads:
#     master: some_script
#   tags:
#     '^RC.+': some_script
# 